<% if current_user.following?(user) %>
  <%= link_to 'Unfollow', user_relationships_path(user.id),class:"btn btn-info", method: :delete %>
<% else %>
  <%= link_to 'Follow', user_relationships_path(user.id),class:"btn btn-success mx-2", method: :post %>
<% end %>